nextflow_workflow {

    name "Test Workflow QC_Reads_Prealign"
    script "subworkflows/qc_reads_prealign.nf"
    workflow "QC_Reads_Prealign"

    test("QC_Reads_Prealign succeeds for reads and cutadapt log -- SE reads.") {
        // instantiate helper objects
        def readsLane1 = new ReadsSELane1()

        when {
            params {
                publishDirReports = "${outputDir}/reports"
                publishDirData    = "${outputDir}/data"
            }
            workflow {
                """
                input[0] = [
                    [
                        sampleName:   "${readsLane1.getSampleName()}",
                        sampleNumber: "${readsLane1.getSampleNumber()}",
                        lane:         "${readsLane1.getLane()}",
                        readType:     "${readsLane1.getReadType()}"
                    ],
                    [
                        file("${readsLane1.getR1()}")
                    ]
                ]
                input[1] = Channel.of(file("https://github.com/ewels/MultiQC_TestData/raw/master/data/modules/cutadapt/v2.10/SAMPLE3_SE.cutadapt.log"))
                """
            }
        }

        then {
            // test workflow status
            assert workflow.success
            assert workflow.trace.succeeded().size() == 2

            // test output for full MultiQC
            assert workflow.out.multiqc
            with(workflow.out.multiqc.get(0)) {
                assert size() == 2
                assert get(0) ==~ /^.*\/${readsLane1.getR1SimpleName()}_fastqc.zip/
                assert get(1) ==~ /^.*\/SAMPLE3_SE.cutadapt.log/
            }
        }

    }

    test("QC_Reads_Prealign succeeds for reads and cutadapt log -- PE reads.") {
        // instantiate helper objects
        def readsLane1 = new ReadsPELane1()

        when {
            params {
                publishDirReports = "${outputDir}/reports"
                publishDirData    = "${outputDir}/data"
            }
            workflow {
                """
                input[0] = [
                    [
                        sampleName:   "${readsLane1.getSampleName()}",
                        sampleNumber: "${readsLane1.getSampleNumber()}",
                        lane:         "${readsLane1.getLane()}",
                        readType:     "${readsLane1.getReadType()}"
                    ],
                    [
                        file("${readsLane1.getR1()}"),
                        file("${readsLane1.getR2()}")
                    ]
                ]
                input[1] = Channel.of(file("https://github.com/ewels/MultiQC_TestData/raw/master/data/modules/cutadapt/v2.10/SAMPLE3_SE.cutadapt.log"))
                """
            }
        }

        then {
            // test workflow status
            assert workflow.success
            assert workflow.trace.succeeded().size() == 2

            // test output for full MultiQC
            assert workflow.out.multiqc
            with(workflow.out.multiqc.get(0)) {
                assert size() == 3
                assert get(0) ==~ /^.*\/${readsLane1.getR1SimpleName()}_fastqc.zip/
                assert get(1) ==~ /^.*\/${readsLane1.getR2SimpleName()}_fastqc.zip/
                assert get(2) ==~ /^.*\/SAMPLE3_SE.cutadapt.log/
            }
        }

    }

}
