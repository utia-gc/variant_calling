docker {
    enabled = true
    temp = 'auto'
}

profile {
    test {
        docker {
            enabled = true
            temp = 'auto'
        }
        params {
            baseDirReport = "${PWD}/report"
            baseDirData   = "${PWD}/data"
        }
    }
}