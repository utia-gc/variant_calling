docker {
    enabled = true
    temp = 'auto'
}

profiles {

    test {

        docker {
            enabled = true
            temp = 'auto'
        }

        params {
            help = null
            
            baseDirReport = "${PWD}/report"
            baseDirData   = "${PWD}/data"
        }
    }
}