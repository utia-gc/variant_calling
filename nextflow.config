docker {
    enabled = true
    temp = 'auto'
}

profiles {

    test {

        docker {
            enabled = true
            temp = 'auto'
        }

        params {
            baseDirReport = "${PWD}/report"
            baseDirData   = "${PWD}/data"
        }
    }
}