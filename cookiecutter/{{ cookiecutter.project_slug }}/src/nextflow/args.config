/** 
 * args.config
 *
 * A Nextflow config file for arguments to processes.
*/


process {
    withName: 'cutadapt' {
        ext.argsUser = [:]
    }

    withName: 'fastp' {
        ext.argsUser = [:]
    }
}
